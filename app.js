function addSomeNumbers( numbers ) {
  var total = 0;

  for (var i = 0; i < numbers.length; i++)
    total += numbers[i];

  return total
}

var numberList = [ 0, 23, -12, ];

addSomeNumber(numberList);
